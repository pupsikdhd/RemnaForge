# URL подключения к базе данных SQLite
DATABASE_URL="file:./dev.db"

# Точное доменное имя вашего сервера (используется для подписей Passkeys и VLESS ссылок)
APP_DOMAIN="your-domain.ru"

# Порт для запуска приложения (по умолчанию 3000)
# PORT=3000
